name: Deploy to Vercel

# Disabled — Vercel Git integration handles deployments automatically
# on every push to master. Re-enable this workflow by adding these
# GitHub secrets if you want CI-triggered deploys:
#   VERCEL_TOKEN       — from vercel.com/account/tokens
#   VERCEL_ORG_ID      — from vercel.com/account (or team settings)
#   VERCEL_PROJECT_ID  — from your project Settings → General

on:
  workflow_dispatch: # manual trigger only
